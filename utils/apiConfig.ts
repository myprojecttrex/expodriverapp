// ═══════════════════════════════════════════════════════════
// API Configuration for Driver App
// ═══════════════════════════════════════════════════════════
// Change this to your backend server's IP/URL.
// - For LAN development: use your PC's local IP (e.g. "http://192.168.1.5:5000")
// - For tunnel mode: use the ngrok URL (e.g. "https://xxxx.ngrok-free.app")
// - For localhost (emulator only): "http://10.0.2.2:5000" (Android) or "http://localhost:5000" (iOS)

export const API_BASE_URL = "https://selah-pyritic-unthematically.ngrok-free.dev";
